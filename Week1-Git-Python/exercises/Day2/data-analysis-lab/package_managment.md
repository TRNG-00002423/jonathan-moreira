Task 1: Install Packages
Q4: # pandas has 3 dependencies: numpy, python-dateutil, tzdata.

Task 4: Freeze Requirements
Versions are pinned to make sure we use the same versions of the dependencies across different enviorments to avoid bugs.